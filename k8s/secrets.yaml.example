# Optional: Pre-configure admin token instead of using first-boot web setup
# 
# To use:
# 1. Copy this file to secrets.yaml
# 2. Replace 'your-secure-admin-token-here' with a secure token
# 3. Apply: kubectl apply -f secrets.yaml
# 4. Uncomment ADMIN_TOKEN in service.yaml
#
# Note: The first-boot web setup is recommended for most deployments.
# This secret is only needed if you want to pre-configure the admin token.
#
apiVersion: v1
kind: Secret
metadata:
  name: digit-link-secrets
  namespace: dzone-dev
type: Opaque
stringData:
  admin-token: your-secure-admin-token-here
