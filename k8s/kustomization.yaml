apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dzone-dev

resources:
  - namespace.yaml
  - service.yaml
  - ingress.yaml
  # Optional: Pre-configure admin token (copy secrets.yaml.example to secrets.yaml)
  # - secrets.yaml
  # NOTE: image-updater.yaml must be applied separately (different namespace)
  # kubectl apply -f k8s/image-updater.yaml
